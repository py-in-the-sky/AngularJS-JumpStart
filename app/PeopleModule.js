(function() {
    angular.module('peopleModule', []);  // square brackets are for listing other module dependencies
}());
// using the closure keeps us from exposing anything to the global environment
